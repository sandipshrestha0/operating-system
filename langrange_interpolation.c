#include<stdio.h>
# define N 15
void main()
{
	float x[N], y[N], x,  res=0, num, den;
	int i,j,n;
	printf("Enter the no of terms: ");
	scanf("%d",&n);
	for(i=0; i<n; i++)
	{
		printf("Enter %d term x and y value:",i);
		scanf("%f%f",&x[i],&y[i]);
	}
	
	}
